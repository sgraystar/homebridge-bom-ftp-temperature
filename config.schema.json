{
  "pluginAlias": "Homebridge BoM FTP Temperature",
  "pluginType": "platform",
  "singular": true,
  "footerDisplay": "FTP Public Products are found at http://www.bom.gov.au/catalogue/anon-ftp.shtml",
  "schema": {
    "name": {
      "title": "Name",
      "type": "string",
      "default": "BoM FTP Temperature"
    },
    "locations": {
      "type": "array",
      "items": {
        "title": "Location",
        "type": "object",
        "properties": {
          "label": {
            "title": "Label",
            "type": "string",
            "required": true,
            "minLength": 1,
            "maxLength": 20,
            "default": "Mornington",
            "description": "Your label for this location e.g. Home, Work, Beach"
          },
          "bomproductid": {
            "title": "BoM Product ID",
            "type": "string",
            "required": true,
            "default": "IDV10702",
            "pattern": "^ID[D,N,Q,S,T,V,W]{1}[0-9]{5}$",
            "description": "Forecast Product ID from BoM FTP Public Products. Format is 3 CAPS, 5 digits, no spaces. Locations that duplicate an existing product id will be ignored."
          },
          "downloaddelay": {
            "title": "Minutes to wait after issue time before attempting download",
            "type": "integer",
            "required": true,
            "default": 7,
            "minimum": 1,
            "maximum": 59,
            "description": "The BoM FTP site is busy at the exact issue time. A short delay increases chances of a successful download."
          }
        }
      }
    }
  }
}
